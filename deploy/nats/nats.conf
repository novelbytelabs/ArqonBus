port: 4222
monitor_port: 8222

jetstream {
    store_dir: "/data"
    max_mem: 1G
    max_file: 5G
}

debug: true
trace: false

# Authorization (Open for Phase 2 dev, will lock down in Phase 4)
authorization {
    # No auth for now to speed up "The Factory" testing
}
